## 1) زر "أرسل طلب شراء" مع نافذة إدخال
- تغيير نص الزر في صفحات التفاصيل:
  - `pages/CarDetailPage.tsx:131-133` → استبدال النص إلى "أرسل طلب شراء" وإضافة `onClick` لفتح نافذة.
  - `pages/PropertyDetailPage.tsx:130-132` → نفس التغيير.
- إنشاء مكوّن نافذة مشتركة `components/orders/PurchaseRequestModal.tsx` يحتوي حقول: الاسم، رقم الهاتف، الملاحظات، مع تحقق بسيط (اسم ورقم مطلوبان).
- عند الإرسال، يستدعي `POST /api/orders` بقيم: `name`, `phone`, `message`, `productType: 'car' | 'property'`, `productId`.
  - نقطة النهاية متاحة علنًا: `server/routes/orderRoutes.js:6`.
- عرض رسالة نجاح/فشل، وإغلاق النافذة وتعطيل زر الإرسال أثناء التنفيذ.

## 2) تزامن الداشبورد مع البيانات الفعلية
- استبدال القيم الثابتة في `pages/admin/AdminDashboard.tsx:51-55` بجلب فعلي:
  - إجمالي السيارات: طول نتيجة `GET /api/cars`.
  - إجمالي العقارات: طول نتيجة `GET /api/properties`.
  - إجمالي الطلبات: طول نتيجة `GET /api/orders` (يتطلب توكن الأدمن؛ المخزن يُحقن تلقائيًا عبر `utils/api.ts:11-16`).
- استبدال مصدر "الطلبات الأخيرة" من البيانات الوهمية `data/mockData.ts` إلى استدعاء فعلي `GET /api/orders` المرتب تنازليًا (`server/controllers/orderController.js:8-11`). وعرض أول 5.
- تعديل جدول "الطلبات الأخيرة" ليعكس نموذج الطلب الحقيقي `server/models/Order.js:3-10`:
  - عرض النوع من `productType`، والعميل من `name`، والهاتف من `phone`.
  - حقل المبلغ غير متوافر حاليًا؛ سنعرض "—" مؤقتًا أو نحسبه بعد إضافة حقل سعر الطلب.
- تحسين وسم الحالة باستخدام القيم العربية الحالية (`جديد`، `قيد المراجعة`، `مكتمل`، `ملغية`).

### إضافة حقل للإيرادات (مقترح لتزامن كامل)
- إضافة حقل `priceAtOrder` إلى نموذج الطلب لتجميع الإيرادات الفعلية:
  - تعديل `server/models/Order.js` بإضافة `priceAtOrder: Number`.
  - عند إنشاء الطلب من النافذة، نجلب سعر المنتج الحالي (`/cars/:id` أو `/properties/:id`) ونرسله ضمن الطلب.
  - في الداشبورد، نحسب "إجمالي الإيرادات" كمجموع `priceAtOrder` للطلبات ذات الحالة `مكتمل`.

## 3) جعل الإعلان "مميز"
- إضافة علم `featured: { type: Boolean, default: false }` في:
  - السيارات: `server/models/Car.js:14` بإضافة الحقل قبل/بعد `available`.
  - العقارات: `server/models/Property.js:14` بنفس الطريقة.
- تحديث نماذج الإدارة لإتاحة التحكم:
  - `pages/admin/ManageCars.tsx:134-152` → إضافة مربع اختيار "إعلان مميز" ضمن نموذج الإضافة وإرساله في الحمولة.
  - `pages/admin/ManageProperties.tsx:115-129` → إضافة مربع اختيار مماثل.
  - دعم التعديل بإرسال `featured` عند التحديث.
- تحسين العرض في البطاقات والقوائم بإظهار شارة "مميز" عند كون الحقل `true` (اختياري في هذه المرحلة).

## 4) تحديث قسم "تابعنا" وإبدال رمز تويتر إلى X
- إضافة روابط وأيقونات YouTube وTikTok:
  - تعديل `components/layout/Footer.tsx:60-65` لإضافة عنصرين جديدين مع `FaYoutube` و`FaTiktok`، ووصلات من `settings.socialLinks`.
- تغيير أيقونة تويتر إلى منصة X:
  - استبدال `FaTwitter` بـ `FaXTwitter` وربط إلى `https://x.com/`.
  - ملاحظة: الأيقونة موجودة في `react-icons/fa6`; في حال عدم توافرها محليًا، سنستخدم `SiX` أو `SiTiktok` من `react-icons/si` كبديل.
- توسيع إعدادات الموقع:
  - في `context/SiteSettingsContext.tsx:21-22` نضيف `tiktok` داخل `socialLinks` الافتراضية وندعمه في الدمج `:89-95`.
  - نموذج إعدادات الإدارة (إن وجد) سيظهر الحقلين الجديدين تلقائيًا لأنه يعتمد على كائن `socialLinks`.

## التحقق والاختبارات
- التحقق يدويًا:
  - من صفحات التفاصيل، فتح النافذة وإرسال طلب؛ التأكد من ظهور السجل في قاعدة البيانات، ومن ظهوره في "الطلبات الأخيرة" بالداشبورد.
  - مراجعة أرقام الإجماليات بعد إضافة/حذف عناصر للتأكد من التزامن.
  - تفعيل/إلغاء "مميز" من النماذج والتأكد من حفظه.
  - عرض روابط YouTube وTikTok وتحوّل رمز X وروابطه.
- لا تغييرات على صلاحيات API عدا استخدام التوكن المخزّن، ونقاط النهاية الحالية تدعم العمليات المطلوبة.

## المخرجات المتوقعة
- زر "أرسل طلب شراء" يعمل ويجمع بيانات العميل ويرسل طلبًا.
- لوحة التحكم تعرض أرقام فعلية و"الطلبات الأخيرة" من قاعدة البيانات.
- إمكانية تعليم الإعلانات كمميزة من خلال الإدارة.
- تحديث قسم "تابعنا" بإضافة YouTube وTikTok واستبدال رمز تويتر بـ X.